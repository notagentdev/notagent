# Faktenbericht: @notagent/coding-agent — Kern (ohne Extension-System)

Quelle: `/Users/dev/projects/notagent-main/packages/coding-agent`. Alle Aussagen aus dem Quellcode (Stand 2026-08-13). Gesamt-LOC in src/: **68 856**.

Workspace-Deps: agent-core, ai, tui, protocol, client. Zusätzlich verifiziert: coding-agent importiert aus `@notagent/agent-core` (Root-Export) nur: Agent, AgentMessage, AgentState, AgentTool, CustomMessage, setDefaultStreamFn, StreamFn, ThinkingLevel, uuidv7 — plus in `src/server/create-harness.ts` (161 LOC) den **Stub** AgentHarness samt Harness-Tools; create-harness.ts wird ausschließlich vom eigenen Test konsumiert, nicht vom Laufzeitpfad.

## 1. Struktur src/
- Root 3 283: cli.ts 21 (Bin-Entry), main.ts 1 062 (CLI-Orchestrierung), rpc-entry.ts 13, index.ts 408 (SDK-Export), config.ts 576 (Pfade/Install-Detection), migrations.ts 314, package-manager-cli.ts 889
- core/ 37 403 — direkt (46 Dateien, 18 800): agent-session.ts 3 797, package-manager.ts 2 677, session-manager.ts 1 714, settings-manager.ts 1 273, resource-loader.ts 1 096, model-runtime.ts 787, model-resolver.ts 769, provider-composer.ts 572, auth-storage.ts 507, skills.ts 487, agent-session-runtime.ts 441, sdk.ts 402, footer-data-provider.ts 388, keybindings.ts 386, system-prompt.ts 173, slash-commands.ts 43, bash-executor.ts 156 u.a.
  - core/tools/ 21 Dateien 6 209; core/extensions/ 3 929 (ENTFÄLLT); core/tasks/ 7 Dateien 1 573; core/compaction/ 1 513; core/mini-read/ 1 433 (tree-sitter-Minifizierung); core/permissions/ 978; core/hooks/ 925; core/export-html/ 746 (+vendor marked.min.js, highlight.min.js); core/modes/ 502 (+builtin auto/manual/plan/yolo als .md); core/delegation/ 408; core/todos/ 387
- modes/ 20 626: interactive-mode.ts 6 688, components/ 47 Dateien 10 406, theme/ 1 474 (theme.ts 1 335, dark.json, light.json), rpc/ 1 765 (rpc-mode.ts 817, rpc-client.ts 601, rpc-types.ts 289, jsonl.ts), print-mode.ts 169, json-event.ts 40
- utils/ 3 553 (Bilder, Clipboard, Git, Pfade, Shell, Highlighting, tools-manager für fd/rg-Download, version-check)
- cli/ 1 831: args.ts 435, session-picker, startup-ui, auth-check, auth-command, credential-print, config-selector, file-processor, initial-message, list-models, project-trust; cli/experimental/ 454 (nur in Tests referenziert)
- client/ 530 (RemoteSession über @notagent/client — nur in Tests instanziiert), server/ 161 (create-harness — nur Test), bun/ 55, extensions/ 1 414 (ENTFÄLLT bis auf native Nachbildung llama)

## 2. Entry/CLI
Kette: dist/cli.js ← src/cli.ts → main(argv) in src/main.ts. rpc-entry.ts ruft main(["--mode","rpc",...]). cli.ts setzt process.title, NOTAGENT_CODING_AGENT=true, AI_AGENT=notagent, undici-Dispatcher.

### Subcommands
Erkennungsreihenfolge in main(): auth → Package-Commands → Config → parseArgs.
- notagent install <source> [-l] / remove|uninstall / update [all|self|extensions|models] / list / config [-l]
- notagent auth check --provider|--model [--json] [--credentials] [--no-refresh] (Exit 0 ready/1 not_ready/2 invalid); auth print-api-key; auth print-bearer-token [--min-expiry 30m|1h]; auth help

### Flags (args.ts:67-241, vollständig)
--help/-h, --version/-v, --mode text|json|rpc, --continue/-c, --resume/-r, --provider, --model (prov/id, :thinking-Suffix), --api-key, --system-prompt, --append-system-prompt (mehrfach, Text/Datei), --name/-n, --no-session, --session (path|id-prefix), --session-id, --fork, --session-dir, --models (Komma, Globs/Fuzzy), --no-tools/-nt, --no-builtin-tools/-nbt, --tools/-t, --exclude-tools/-xt, --thinking off..max, --auto, --yolo (Konflikt mit --auto=Fehler), --print/-p [msg], --export file, --extension/-e (ENTFÄLLT), --no-extensions/-ne (ENTFÄLLT), --skill (mehrfach), --no-skills/-ns, --prompt-template, --no-prompt-templates/-np, --theme, --no-themes, --no-context-files/-nc, --list-models [pattern], --verbose, --tui-mode regular|fullscreen, --approve/-a, --no-approve/-na, --offline (setzt NOTAGENT_OFFLINE=1 + SKIP_VERSION_CHECK), @<pfad> → fileArgs, --<unbekannt> → unknownFlags (Extension-Flags: ENTFÄLLT), freie Argumente → messages.

### Modi (resolveAppMode)
rpc | json | print | interactive: --mode rpc → rpc; --mode json → json; --print ODER !stdin.isTTY ODER !stdout.isTTY → print; sonst interactive. Gepipetes stdin (außer RPC) schaltet interactive→print. takeOverStdout() für Nicht-Interactive. Sonderpfade: --version, --export, --help (nach Extension-Load), --list-models, NOTAGENT_STARTUP_BENCHMARK=1.

### Env-Variablen
Provider-Keys (siehe args.ts:376-424): ANTHROPIC_{AUTH_TOKEN,API_KEY,OAUTH_TOKEN}, OPENAI_API_KEY, AZURE_OPENAI_*, GEMINI, GROQ, CEREBRAS, XAI, FIREWORKS, TOGETHER, BASETEN, OPENROUTER, AI_GATEWAY, ZAI*, MISTRAL, MINIMAX, MOONSHOT, OPENCODE, KIMI, CLOUDFLARE_*, QWEN_*, XIAOMI_*, DEEPSEEK, NVIDIA, ANT_LING, AWS_*.
App: NOTAGENT_CODING_AGENT_DIR, NOTAGENT_CODING_AGENT_SESSION_DIR, NOTAGENT_PACKAGE_DIR, NOTAGENT_OFFLINE, NOTAGENT_TELEMETRY, NOTAGENT_SHARE_VIEWER_URL, NOTAGENT_EXPERIMENTAL, NOTAGENT_SUBAGENT_TIMEOUT_MS, NOTAGENT_STARTUP_BENCHMARK, NOTAGENT_SKIP_VERSION_CHECK.
Bash exportiert in Kinder: NOTAGENT_SESSION_ID, NOTAGENT_SESSION_FILE, NOTAGENT_PROVIDER, NOTAGENT_MODEL, NOTAGENT_REASONING_LEVEL.

## 3. Built-in Tools (16, core/tools/index.ts)
read, read_minified, bash, edit, patch_minified, multi_patch_minified, skill, task, task_list, task_output, task_stop, todo_write, write, grep, find, ls. Presets: createCodingTools, createReadOnlyTools. Jedes Tool = ToolDefinition mit name, label, description (teils Getter!), promptSnippet, promptGuidelines, parameters (TypeBox), execute, renderCall, renderResult. constrainedSampling nur bei NOTAGENT_EXPERIMENTAL=1.
Truncation gemeinsam: DEFAULT_MAX_LINES 2000, DEFAULT_MAX_BYTES 50 KiB, GREP_MAX_LINE_LENGTH 500; zwei unabhängige Limits, nie Teilzeilen (Ausnahme bash-Tail).

- **read** {path, offset? (1-basiert), limit?}: resolveReadPathAsync mit macOS-Screenshot-Fallbacks (NBSP, NFD, U+2019); Bilder per MIME → processImage (Auto-Resize max 2000×2000) + Hinweis bei Nicht-Vision-Modell; truncateHead; „Use offset=N to continue"-Notiz.
- **read_minified** {path, offset?, limit?, keep_comments?}: Kommentare/Leerzeilen weg, Einrückung 1 Space/Ebene; Sprachen (tree-sitter-wasm): Rust, Python, JS/JSX, TS/TSX, Go, Java, C, C++, Ruby, Bash, CSS, HTML, JSON; Fallback Rohinhalt; keine Zeilennummern (kein edit.oldText daraus!).
- **bash** {command, timeout? s} + (nur wenn Background erlaubt) {run_in_background?, description?, disable_timeout?} — parameters/description sind GETTER (Schema wechselt!). **Kein PTY**: spawn(shell, args, stdio pipe, detached auf Nicht-Windows, windowsHide). Shell: settings.shellPath → Git-Bash (Win) → /bin/bash → which bash → sh; Legacy-WSL bash.exe → stdin-Transport. Timeouts: 60 s Default / 300 s Max; Background 600 s / 86 400 s; geklemmt. **Auto-Backgrounding**: Deadline erreicht + Background erlaubt → detach statt kill, task_id + „output so far". Abbruch: terminateProcessTree → nach 2 000 ms killProcessTree. OutputAccumulator: Tempfile notagent-bash*, Updates 100 ms throttled, Preview 5 Zeilen. Exit≠0 → Fehler + Output. shellCommandPrefix aus Settings; spawnHook (command/cwd/env). BashOperations austauschbar.
- **edit** {path, edits:[{oldText,newText}]}: withFileMutationQueue (Serialisierung pro realpath, Abort-Check nach jedem await); BOM abtrennen, Zeilenende erkennen (erstes CRLF vs LF), auf LF normalisieren; fuzzyFindText: exakt → Fuzzy (NFKC, trailing WS je Zeile, Smart-Quotes→'/", Unicode-Dashes→-, Spezial-Spaces→Space); jedes oldText muss EINDEUTIG sein (Count>1 → Fehler); alle Edits gegen ORIGINAL gematcht; Überlappung → Fehler; Anwendung rückwärts; bei Fuzzy nur betroffener Zeilenblock ersetzt (Original-Bytes sonst erhalten); identisches Ergebnis → „No changes made"; Zeilenenden + BOM restauriert. details={diff (Display, 4 Kontextzeilen), patch (Unified), firstChangedLine}. Legacy: edits als JSON-String, Top-Level oldText/newText. Preview via computeEditsDiff während des Streamings.
- **patch_minified** {path, old_string, new_string, replace_all?, keep_comments?} / **multi_patch_minified** {path, edits[], keep_comments?}: Suche im minifizierten Raum, Source-Map-Rückabbildung; multi re-minifiziert zwischen Schritten; Schreiben erst wenn alle Edits ok; Fallback applyRawReplace. details mit warnings[].
- **write** {path, content}: mkdir -p, überschreibt, UTF-8, via FileMutationQueue. Rendering mit inkrementellem Highlight-Cache.
- **grep** {pattern, path?, glob?, ignoreCase?, literal?, context?, limit?}: **ripgrep** (ensureTool("rg") lädt nach ~/.notagent/agent/bin), --json --line-number --color=never --hidden; limit default 100 (rg wird gekillt); Kontextzeilen aus Datei nachgelesen; Zeilen >500 gekürzt; Format pfad:zeile: bzw. pfad-zeile-.
- **find** {pattern, path?, limit?=1000}: **fd**, --glob --color=never --hidden --max-results; ohne Git-Repo --no-require-git; Muster mit / → --full-path + ggf. **/-Präfix; Windows / → [/\\].
- **ls** {path?, limit?=500}: alphabetisch case-insensitiv, Verzeichnisse mit /, Dotfiles inkl.
- **skill** {name}: erst Modes (id), dann Skills (name), case-insensitiv; Envelope <skill name path>body<resources/></skill>; Geschwisterdateien nur gelistet. details {name, tokens, resources}.
- **task** {tasks: string[], mode, session_id?} + dyn. {run_in_background?} — siehe §5.
- **task_list** {all?, limit?=20} / **task_output** {task_id} (Tail 32 KiB, wartet NIE, meldet output_path) / **task_stop** {task_id, reason?} (unterdrückt erst Notification). Alle drei werfen ohne TaskManager (Subagenten).
- **todo_write** {todos:[{content, activeForm, status pending|in_progress|completed}]}: jeder Call ERSETZT die Liste; [] löscht; alle completed → zurückgeben + auto-leeren; MAX_TODO_LENGTH 1000; details {before, after}; Diff-Rendering.

## 4. Session-Management (core/session-manager.ts 1 714)
**JSONL, Baum, append-only. CURRENT_SESSION_VERSION = 3.**
Zeile 1 Header {type:"session", version:3, id, timestamp, cwd, parentSession?}. Entries mit {id, parentId, timestamp}: message {message}, thinking_level_change, model_change {provider, modelId}, compaction {summary, firstKeptEntryId, tokensBefore, details?, usage?, fromHook?}, branch_summary {fromId, summary, ...}, custom {customType, data?} (NICHT im LLM-Kontext), custom_message {customType, content, details?, display} (WIRD Kontext), label {targetId, label}, session_info {name?}.
Entry-IDs: 8 Hex aus randomUUID, kollisionsgeprüft. Session-IDs: uuidv7, validiert.
Ort: <agentDir>/sessions/--<cwd-escaped>--/<ISO mit :.→->_<id>.jsonl; agentDir = $NOTAGENT_CODING_AGENT_DIR oder ~/.notagent/agent. Override: --session-dir → $NOTAGENT_CODING_AGENT_SESSION_DIR → settings.sessionDir → Default.
_persist(): Datei erst bei erster Assistant-Message (openSync "wx", danach appendFileSync). Migrationen v1→v2→v3 schreiben Datei neu.
open(): Header-Scan max 1 MiB, Fallback Vollladen. continueRecent(): jüngste mtime. inMemory(): --no-session. forkFrom(): NEUE Datei, parentSession=Quelle, Entries kopiert. list/listAll: parallel, max 10 gleichzeitig.
CLI-Auflösung resolveSessionPath: /,\ oder .jsonl → Pfad; sonst exakt → Präfix im Projekt → global (mit Rückfrage „Fork this session into current directory?").
Baum: leafId; appendXxx hängt an + rückt vor; branch(id) setzt zurück; branchWithSummary; createBranchedSession(leafId) = neue Datei nur mit Pfad Wurzel→Leaf; getTree() (Kinder nach Timestamp, Orphans als Roots). Nichts wird gelöscht/modifiziert.
Kontextaufbau buildContextEntries: Pfad Wurzel→Leaf; letzte Compaction gewinnt: [compaction] ++ [ab firstKeptEntryId bis vor Compaction] ++ [nach Compaction]. buildSessionContext liefert zusätzlich thinkingLevel (Default off) und model.

### Kompaktierung (core/compaction/, 1 513)
DEFAULT: enabled, reserveTokens 16384, keepRecentTokens 20000. Trigger: contextTokens > contextWindow - reserveTokens; Tokens = usage.totalTokens || Summe; Schätzung ceil(chars/4), Bild=4800.
Aufruf _checkCompaction (agent-session.ts:2354) nach agent_end und vor Prompt: 1. Recoverable failure (Overflow oder length-Stop, nur gleiches Modell): fehlgeschlagene Assistant-Message aus Agent-State entfernen (bleibt in History), kompaktieren, EINMAL Auto-Retry (_overflowRecoveryAttempted). 2. Threshold: kompaktieren ohne Retry. Skip wenn Assistant-Message älter als letzte Compaction-Grenze.
findCutPoint: rückwärts akkumulieren bis keepRecentTokens; gültige Cut-Points: user, assistant, bashExecution, custom, branchSummary, compactionSummary — NIE toolResult; angrenzende Metadaten einschließen; Split-Turn → turnStartIndex.
Prompts: SUMMARIZATION_PROMPT / UPDATE_SUMMARIZATION_PROMPT (mit <previous-summary>), festes Markdown-Schema (## Goal, ## Constraints & Preferences, ## Progress mit Done/In Progress/Blocked, ## Key Decisions, ## Next Steps, ## Critical Context); Konversation in <conversation> serialisiert; maxTokens = min(0.8·reserveTokens, model.maxTokens); Split-Turn zusätzlich TURN_PREFIX-Prompt (0.5·reserve). Datei-Operationen (readFiles/modifiedFiles) kumulativ in details. Call via retryAssistantCall mit cacheRetention "none" + eigener sessionId.

## 5. Subagenten / Tasks
Dateien: core/tools/task.ts 394, core/delegation/run.ts 324 + limits.ts 84, core/tasks/ (manager.ts 616, store.ts, shell-task.ts, subagent-task.ts, notification.ts, output.ts, types.ts).
Subagent = derselbe Agent in anderem Mode; Mode = Agent-Typ. Schema: tasks[] (max 8 = MAX_DELEGATIONS_PER_CALL, parallel), mode, session_id? (Fortsetzung, nur bei 1 Task), run_in_background? (nur wenn task_*-Tools aktiv). description ist Getter (listet Modes + Tools zur Laufzeit).
Regeln (Code-erzwungen): 1. TOOLS_WITHHELD_FROM_CHILDREN = {task, task_list, task_output, task_stop, todo_write} — keine Rekursion. 2. exceedsParent(): kein Shell-Upgrade (read-only-Parent → nur read-only-Kinder). 3. mode.subagents-Allowlist. 4. delegationFingerprint gegen Duplikate im Call. 5. DELEGATION_TIMEOUT_MS 2h (NOTAGENT_SUBAGENT_TIMEOUT_MS, 0=aus); Timeout → KEIN Teiltext, Aufforderung zur Fortsetzung mit session_id. 6. MAX_ANSWER_CHARS 100 000 (Kürzung mit Hinweis), MIN_ANSWER_CHARS 200 → genau EIN EXPANSION_PROMPT-Nachfrage-Turn.
Parallelität: alle tasks starten sofort, je eigener AbortController, Registrierung beim TaskManager; Vordergrund Promise.all über waitForForegroundRelease (Release "detached"/"timeout_detached" = Nutzer hat detacht). Ergebnis: <subagent mode session task>text</subagent>. Hintergrund: sofort task_id/session_id/mode/status running + Anweisung NICHT zu pollen.
Fortsetzung: WeakMap<TaskToolSources, Map<sessionId, {modeId, messages}>> — prozesslokal, nicht persistiert; Mode muss übereinstimmen; Mode-Block wird bei Fortsetzung nicht wiederholt.
createChild: neue Agent-Instanz mit systemPrompt/model/thinkingLevel des Parents; übernimmt convertToLlm, streamFn, getApiKey, onPayload, onResponse, before/afterToolCall, thinkingBudgets, transport, maxRetryDelayMs, toolExecution; EIGENE sessionId; Tools bevorzugt aus Eltern-Registry (resolveTool), sonst createTool. Erster Prompt: renderChildPrompt = <mode name shell>body</mode> + Task. runDelegation rejectet NIE. Token-Zählung: message_end-Usage aufsummiert, live gemeldet.
TaskManager (Shell+Subagent gemeinsam): IDs <prefix>-<8 base36>; Status running|completed|failed|timed_out|killed|lost (lost via reconcile beim Prozessstart); Stop dreistufig abort → 5 000 ms → forceStop; maxRunningTasks vorgesehen aber nicht gesetzt; TaskStore ~/.notagent/agent/tasks/<sessionId>/ (<taskId>.json atomar via rename + output.log; Vordergrund ohne Log bis Detach); Notifications via TaskNotifier → sendCustomMessage; TaskManager pro sessionId, Session-Wechsel → shutdown("Session replaced").

## 6. Interaktiver Modus (interactive-mode.ts 6 688)
### Slash-Commands (BUILTIN_SLASH_COMMANDS + Dispatcher :3113-3245)
/settings, /model [suchbegriff], /scoped-models, /export [pfad .html|.jsonl], /import <pfad.jsonl>, /share (GitHub-Gist), /copy, /name <name>, /session, /tasks (/task), /changelog, /hotkeys, /fork, /clone, /tree, /trust, /login [provider], /logout, /new, /compact [instructions], /resume, /reload, /quit. Zusätzlich im Dispatcher: /debug + Easter Eggs (/arminsayshi, /dementedelves). Dynamisch: /<prompt-template-name>, /skill:<name> (wenn enableSkillCommands).
### Editor & Autocomplete
CustomEditor extends Editor (tui); actionHandlers Map<AppKeybinding, fn> (false → Weitergabe); onEscape, onCtrlD (nur leerer Editor), onPasteImage; Escape nicht abgefangen solange Autocomplete offen; explizite History-Bindings vor App-Actions. CombinedAutocompleteProvider(slashCommands, cwd, fdPath); /model und /login mit Argument-Completions; autocompleteMaxVisible Default 5. Externer Editor Ctrl+G ($VISUAL/$EDITOR/settings.externalEditor).
### Bash-Modus (!)
Text beginnt mit ! → Border-Farbwechsel. !cmd → Ausführen + in Kontext; !!cmd → excludeFromContext. Läuft bereits eins → Warnung. Via AgentSession.executeBash() → core/bash-executor.ts (Streaming, ANSI-Strip, Binär-Sanitizing, \r entfernt, Rolling-Buffer 2×50 KiB, Tempfile ab 50 KiB).
### File-Mentions: CLI @pfad → <file name="ABS">…</file> bzw. ImageContent; TUI: fd-Autocomplete (kein eigener Injektionsmechanismus).
### Queueing
Submit während Streaming → steer; Alt+Enter → followUp; Alt+Up → dequeue zurück in Editor. Während Compaction separat gequeued. settings.steeringMode/followUpMode: all | one-at-a-time.
### Keybindings (core/keybindings.ts)
KeybindingsManager extends TuiKeybindingsManager; ~/.notagent/agent/keybindings.json; 60 Legacy-Migrationen. 45 App-Actions mit Defaults: app.interrupt escape, app.clear ctrl+c, app.exit ctrl+d, app.suspend ctrl+z (leer auf Win), app.mode.cycle shift+tab, app.thinking.cycle ctrl+t, app.thinking.toggle shift+ctrl+t, app.model.cycleForward ctrl+p / Backward shift+ctrl+p, app.model.select ctrl+l, app.tools.expand ctrl+o, app.tasks.cycle alt+b, app.tasks.detach ctrl+b, app.session.toggleNamedFilter ctrl+n, app.editor.external ctrl+g, app.message.copy ctrl+x, app.message.followUp alt+enter, app.message.dequeue alt+up, app.clipboard.pasteImage ctrl+v (alt+v Win), app.session.{new,tree,fork,resume} leer, app.tree.* (fold/unfold alt+left/right bzw. ctrl+ auf macOS, editLabel shift+l, toggleLabelTimestamp shift+t), Session-Picker-Actions (togglePath ctrl+p, toggleSort ctrl+s, rename ctrl+r, delete ctrl+d, deleteNoninvasive ctrl+backspace), Models-Selector (save ctrl+s, enableAll ctrl+a, clearAll ctrl+x, toggleProvider ctrl+p, reorderUp/Down alt+up/down), Tree-Filter (default ctrl+d, noTools ctrl+t, userOnly ctrl+u, labeledOnly ctrl+l, all ctrl+a, cycle ctrl+o/shift+ctrl+o). Doppel-Escape: settings.doubleEscapeAction fork|tree|none (Default tree).
### Themes (modes/interactive/theme/)
theme.ts 1 335: TypeBox-Schema mit name, vars (Aliase), colors: Hex | Var-Ref | 256er-Index. Slots: Core UI (accent, border, borderAccent, borderMuted, success, error, warning, muted, dim, text, thinkingText), Backgrounds (selectedBg, scrollbarThumb?, searchMatchBg/Text?, userMessageBg/Text, customMessageBg/Text/Label, toolPendingBg, toolSuccessBg, toolErrorBg, toolTitle, toolOutput), Markdown (10), Diff (3), Syntax (9: comment, keyword, function, variable, string, number, type, operator, punctuation), Thinking-Level-Borders (6). dark.json/light.json mitgeliefert; User-Themes ~/.notagent/agent/themes/; Live-Reload-Watcher. Syntax-Highlighting: highlight.js 10.7.3 (utils/syntax-highlight.ts).
### Komponenten (47 Dateien, 10 406 LOC)
Größte: tree-selector 1 437, session-selector 1 031, config-selector 942, settings-selector 881, tasks-browser 435, scoped-models-selector 403, tool-execution 377, model-selector 364, footer 253, login-dialog 233, bash-execution 220, todo-list 216, oauth-selector 206, assistant-message 197. Weitere: approval-selector, trust-selector, theme-selector, thinking-selector, show-images-selector, user-message-selector, first-time-setup, subagent-panel, tasks-panel, status-indicator, countdown-timer, bordered-loader, dynamic-border, diff, mermaid, markdown-transform, visual-truncate, keybinding-hints, custom-message/entry, compaction-/branch-summary-message, skill-invocation-message, user-message, armin/daxnuts/earendil (Easter Eggs).
TUI-Modi regular|fullscreen; settings.fullscreenExitOutput transcript|resume-hint.

## 7. Konfiguration
### Pfade (config.ts): agentDir = $NOTAGENT_CODING_AGENT_DIR || ~/.notagent/agent; CONFIG_DIR_NAME ".notagent". settings.json (global + <cwd>/.notagent/, deep merge, Projekt nur bei Trust), auth.json, models.json, keybindings.json, themes/, prompts/, sessions/<encoded-cwd>/*.jsonl, tasks/<sessionId>/, bin/ (fd, rg), notagent-debug.log, hooks.json (beide Ebenen), modes/ (beide Ebenen).
### Settings (settings-manager.ts:90-138, vollständig)
lastChangelogVersion, defaultProvider, defaultModel, defaultThinkingLevel, transport, steeringMode, followUpMode, theme, compaction{enabled,reserveTokens,keepRecentTokens}, branchSummary{reserveTokens,skipPrompt}, retry{enabled,maxRetries,baseDelayMs,provider{timeoutMs,maxRetries,maxRetryDelayMs}}, hideThinkingBlock, showCacheMissNotices, externalEditor, shellPath, quietStartup, defaultProjectTrust (ask|always|never, nur global), shellCommandPrefix, npmCommand[], enableInstallTelemetry, enableAnalytics, trackingId, packages[], extensions[] (ENTFÄLLT), skills[], prompts[], themes[], enableSkillCommands, terminal{showImages,imageWidthCells,clearOnShrink,showTerminalProgress}, images{autoResize,blockImages}, enabledModels[], doubleEscapeAction, treeFilterMode, thinkingBudgets{minimal,low,medium,high}, editorPaddingX, outputPad, autocompleteMaxVisible, showHardwareCursor, markdown{codeBlockIndent,mermaid}, warnings{anthropicExtraUsage}, sessionDir, httpProxy, httpIdleTimeoutMs, websocketConnectTimeoutMs, tuiMode, fullscreenExitOutput, fullscreenScrollbar.
Defaults: transport auto; compaction true/16384/20000; branchSummary 16384; retry true/3/2000; provider.maxRetryDelayMs 60000; showImages true; imageWidthCells 60; clearOnShrink false; showTerminalProgress false; autoResize true; blockImages false; defaultProjectTrust ask; enableInstallTelemetry true; enableAnalytics false; enableSkillCommands true; doubleEscapeAction tree; codeBlockIndent 2 Spaces; mermaid streaming; anthropicExtraUsage true; editorPaddingX 0; outputPad 1; autocompleteMaxVisible 5; tuiMode regular; fullscreenExitOutput transcript; fullscreenScrollbar auto. FileSettingsStorage mit proper-lockfile (10×20 ms Retry). Migrationen: queueMode→steeringMode, websockets→transport, skills-Objekt→Array.
### AGENTS.md/CLAUDE.md (resource-loader.ts:71-156)
Kandidaten pro Verzeichnis (erster gewinnt): AGENTS.override.md, AGENTS.md, AGENTS.MD, CLAUDE.md, CLAUDE.MD. Reihenfolge: agentDir-Datei zuerst, dann Ancestor→cwd (aufwärts gesammelt, unshift). Worktree-Shadowing: linked Worktree mit eigener Kontextdatei lässt gleichnamige Haupt-Repo-Datei aus (findShadowedContextFile, via realpath). --no-context-files deaktiviert.
### System-Prompt (system-prompt.ts)
buildSystemPrompt: 1. Einleitung, 2. Available tools (nur mit promptSnippet), 3. Operating-modes-Absatz, 4. Guidelines (dedupliziert; bash-Hinweis nur wenn bash ohne grep/find/ls; immer „Be concise…" + „Show file paths…"), 5. Notagent-Doku-Pfade, 6. appendSystemPrompt, 7. <project_context> mit <project_instructions path>, 8. Skills (nur wenn read aktiv), 9. Current working directory. --system-prompt ersetzt 1-5, 6-9 bleiben.
### Prompt-Templates (core/prompt-templates.ts 285)
Markdown + YAML-Frontmatter (description, argumentHint); Quellen: ~/.notagent/agent/prompts/, <cwd>/.notagent/prompts/, settings.prompts[], --prompt-template. Substitution: $1..$N, $@, $ARGUMENTS, ${N:-def}, ${@:-def}, ${@:N}, ${@:N:L}; Quotes respektiert; nicht rekursiv.
### Skills (core/skills.ts 487)
SKILL.md, Frontmatter name (≤64, ^[a-z0-9-]+$, keine Doppel-Bindestriche), description (≤1024), disable-model-invocation. Discovery respektiert .gitignore/.ignore/.fdignore. /skill:<name> expandiert zu <skill name location>-Envelope (agent-session.ts:1697).
### Modes (core/modes/modes.ts)
Mode = VERZEICHNIS von .md (alphabetisch, numerische Präfixe); Frontmatter shell (read-only|worker), approval (manual|auto|yolo), tools (Delta +/-), subagents (Allowlist, *). Erste Deklaration gewinnt, Abweichung = Diagnostic; ohne shell → read-only + Warnung. Suchreihenfolge (später gewinnt ganz): builtin → ~/.notagent/agent/modes/ → <cwd>/.notagent/modes/. Builtin: manual (worker/manual), auto (worker/auto), yolo (worker/yolo), plan (read-only). Shells: read-only = read, read_minified, grep, find, ls, skill, task, task_list, task_output, task_stop, todo_write (KEIN bash); worker = + bash, edit, write, patch_minified, multi_patch_minified. applyToolDelta verweigert mutierende Tools für read-only. Aktivierung: renderModeBlock → <mode name shell>…</mode> als verstecktes custom-Message (MODE_BLOCK_TYPE, display:false) direkt vor nächster User-Message (agent-session.ts:1583-1600).

## 8. Weitere Kernfeatures
- **Modell-Auswahl** (model-resolver.ts 769): resolveCliModel (--provider+--model, --model p/m, Fuzzy); parseModelPattern (letzter : als Thinking-Level, rekursiv — wegen IDs wie model:exacto); resolveModelScope → ScopedModel[] für Ctrl+P (beste Version: Alias vor datiert, sonst neueste); Muster aus --models sonst settings.enabledModels; defaultModelPerProvider; Initialwahl: CLI → erstes Scoped (wenn nicht continue/resume) → Session → gespeicherter Default → erstes verfügbares mit Key.
- **Auth**: auth-storage.ts 507 → auth.json; ReadOnlyAuthStorage für --no-refresh; /login, /logout; --api-key (nicht persistent, modelRuntime.setRuntimeApiKey).
- **Checkpoints/Undo: NICHT vorhanden.** Äquivalent = Session-Baum (/fork, /tree, /clone, --fork).
- **Hooks** (core/hooks/ 925): hooks.json (agentDir + <cwd>/.notagent/); Eintrag {event, matcher?, command, timeout? (30 s Default, 300 s Max)}. Events: PreToolUse, PostToolUse, PostToolUseFailure, PermissionRequest, PermissionResult, UserPromptSubmit, Stop, StopFailure, Interrupt, SessionStart, SessionEnd, SubagentStart, SubagentStop (beide noch nicht emittiert), PreCompact, PostCompact, Notification. Matcher nur bei Tool-Events. NUR PreToolUse kann blockieren. Hooks werden in main.ts VOR allem anderen geladen.
- **Permissions** (core/permissions/ 978): Achsen shell + approval (manual|auto|yolo). POLICY_ORDER: 1. auto-mode-ask-user-deny (Frage-Tools in auto verweigert), 2. user-configured-deny (PreToolUse-Hook), 3. destructive-command-ask (VOR yolo!), 4. yolo-mode-approve, 5. session-approval-history (nie bei destruktiv), 6. user-configured-ask, 7. user-configured-allow, 8. sensitive-file-access-ask, 9. git-control-path-access-ask, 10. auto-mode-approve, 11. default-tool-approve (read-only), 12. git-cwd-write-approve, 13. fallback-ask. Read-only-Tools (nie fragen): read, read_minified, grep, find, ls, skill, task, task_list, task_output, todo_write — task_stop bewusst NICHT. Sensible Pfade: .env*, .netrc, .npmrc, .ssh/, .aws/, .gnupg/, id_*, .bashrc/.zshrc/…, *.pem/key/p12/pfx/keystore, credentials*/secrets*. Destruktiv (Regex): rm -rf-Varianten, rmdir, git push --force, git reset --hard, git clean -f, git checkout --, git branch -D, dd of=, mkfs, shutdown/reboot/halt, chmod 777, drop/truncate table, kill -9, npm publish, curl|wget…|sh, >/dev/sd*. Ohne TTY: ask → deny (non-interaktiv braucht --auto/--yolo).
- **Project Trust**: project-trust.ts, trust-manager.ts; defaultProjectTrust ask|always|never; -a/-na; /trust; Projekt-Ressourcen erst nach Trust.
- **MCP: NICHT vorhanden.**
- **Retry** (agent-session.ts:3100-3220): _isRetryableError, _prepareRetry, abortRetry, autoRetryEnabled; retry{enabled,3,2000} exponentiell + Provider-Ebene.
- **HTML-Export** (core/export-html/ 746 + Vendor): /export, --export, RPC export_html; ANSI→HTML; Template html/css/js.
- **JSONL-Export/Import**: /export foo.jsonl, /import.
- **Share**: /share → geheimer GitHub-Gist; Viewer $NOTAGENT_SHARE_VIEWER_URL (Default https://notagent.dev/session/#<gistId>).
- **Statistik**: getSessionStats, getContextUsage, cache-stats, usage-totals, footer-data-provider (Git-Branch).
- **Install-Telemetrie** (core/telemetry.ts 12 LOC): Version-Ping nach Update; enableInstallTelemetry (opt-out), NOTAGENT_TELEMETRY.
- **Version-Check/Self-Update**: utils/version-check.ts, getSelfUpdateCommand (npm/pnpm/yarn/bun/bun-binary), windows-self-update.
- **Tool-Bootstrap**: utils/tools-manager.ts ensureTool("fd"|"rg") lädt nach ~/.notagent/agent/bin.
- **Bilder**: utils/image-*.ts, photon.ts (WASM), exif-orientation, clipboard-image; Auto-Resize 2000×2000; blockImages.
- **Mermaid** (components/mermaid.ts, grok-mermaid): off|final|streaming.
- **Diagnostics/Timings**: core/diagnostics.ts, core/timings.ts, core/output-guard.ts (takeOverStdout/restoreStdout mit Backpressure).
- **Migrationen** (src/migrations.ts): oauth.json+apiKeys→auth.json; Sessions→sessions/<cwd>/; commands/→prompts/; tools/→bin/; Keybinding-Namen.
- **RPC-Modus** (modes/rpc/): JSONL über stdio (EIGENES Protokoll, nicht @notagent/protocol); rpc-mode.ts 817, rpc-client.ts 601, rpc-types.ts 289.

## 9. Extension-Abgrenzung
Siehe plans/facts/extension-boundary.md. Kern-Berührungspunkte, die nativ bleiben müssen: Permissions + Hooks als versteckte Inline-Extensions (main.ts:616-643, Permission zuerst); alle Tools als ToolDefinition mit renderCall/renderResult; ResourceLoader lädt Skills/Prompts/Themes/Context-Files (Extensions-Teil entfällt); package-manager behält skills/prompts/themes.
